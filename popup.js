document.addEventListener('DOMContentLoaded', () => {
  const statusEl = document.getElementById('status');

  // 저장 완료 표시 (언어 고정이므로 별도 설정 불필요)
  // 향후 설정 확장 시 이 파일에서 관리
  _ = statusEl; // reserved
});
